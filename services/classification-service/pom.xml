<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.7</version>
		<relativePath/>
	</parent>
	<groupId>ma.dev</groupId>
	<artifactId>classification-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>classification-service</name>
	<description>Reads a ticket and suggests what it is</description>

	<properties>
		<java.version>21</java.version>
		<spring-cloud.version>2025.1.1</spring-cloud.version>
		<!-- 2.0.x is the line that supports Boot 4.0.x and 4.1.x. Spring AI 1.x is Boot 3 only. -->
		<spring-ai.version>2.0.1</spring-ai.version>
	</properties>

	<dependencies>
		<!--
			Groq speaks OpenAI's protocol, so the OpenAI starter IS the Groq client. There is no
			Groq-specific library and none is needed: only the base URL changes.
		-->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-openai</artifactId>
		</dependency>
		<!--
			M4. Embeddings computed in this process, from an ONNX model, with no API key and no
			network call. Groq has no embeddings endpoint - the architecture diagram claimed it did
			and was wrong - and this turned out to be the better answer anyway: no cost, no rate
			limit, and a similarity search that still works with the wifi off.
			all-MiniLM-L6-v2, 384 dimensions, about 80MB downloaded once and then cached.
		-->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-model-transformers</artifactId>
		</dependency>
		<!-- Stores those vectors and answers "what is nearest to this". -->
		<dependency>
			<groupId>org.springframework.ai</groupId>
			<artifactId>spring-ai-starter-vector-store-pgvector</artifactId>
		</dependency>
		<!--
			PgVectorStore works through JdbcTemplate. No JPA: this service has no entities, and an
			ORM over one library-owned table would be furniture.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-jdbc</artifactId>
		</dependency>
		<!-- The vector table is created by a migration, not by Spring AI. See RabbitConfig's
		     sibling reasoning: a table that appears by itself is one nobody can explain later. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-liquibase</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Consumes issue.created, publishes issue.classified. The service's whole input and output. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-amqp</artifactId>
		</dependency>
		<!--
			There is no controller worth the name here - this is a queue worker. Web is on the
			classpath for actuator's health endpoint and for the admin replay route, which is what
			makes the dead-letter queue operable instead of just observable.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<!-- Validates the /similar query parameters. Rejecting a two-character search at the door
		     is cheaper than embedding it and returning noise. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<!--
			Verifies the same token the other three services do. Replaying a dead-letter queue has
			real effects, so it is an ADMIN action, not a URL anyone who guesses the path can hit.
			This service only ever verifies a token; it never issues one.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<!-- Retry and circuit breaker around the Groq call: it is external and it is flaky. -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
		</dependency>
		<!--
			Registers with Eureka. Nothing calls this service synchronously at M3 - it is driven
			entirely by the queue - but the architecture diagram has it in the registry, and M4's
			similarity search is a synchronous route through the gateway that will need it.
		-->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!--
			Since M4 this service does have a database, vectordb, but it holds no domain state:
			one vector per issue, derived from the issue and rebuildable from scratch by replaying
			issue.created. The AIClassification row still belongs to work-service, next to the
			issue it describes.
		-->

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.springframework.ai</groupId>
				<artifactId>spring-ai-bom</artifactId>
				<version>${spring-ai.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
